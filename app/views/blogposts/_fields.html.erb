<%= render 'shared/error_messages', object: f.object %>

<%= f.label :title %>
<%= f.text_field :title, placeholder: "Add a title..." %>

<%= f.label :content %>
<%= f.text_area :content, placeholder: "Add post content..." %>

<%= f.check_box :published %>
<%= f.label :published, "Publish Post?" %>
