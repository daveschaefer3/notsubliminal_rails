<% provide(:title, 'Add Album') %>
<h1>Add an Album</h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for(@album) do |f| %>
      <%= render 'fields', f: f %>

      <%= f.submit "Add Album", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>